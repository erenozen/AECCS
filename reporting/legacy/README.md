Legacy root-level reports and figures were moved here to quarantine historical
outputs that predate the current `mock` / `real` separation.

Only `reporting/mock/` and `reporting/real/` should be used for current runs.
